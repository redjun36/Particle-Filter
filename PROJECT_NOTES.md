# Depth Particle — 작업 핸드오프 노트

다른 디바이스/새 세션에서 이어서 작업할 때, 이 파일을 먼저 읽고 시작하면 지금까지의
맥락(왜 이렇게 만들었는지, 뭘 시도했다가 버렸는지)을 다시 설명할 필요 없이 이어갈 수 있습니다.

새 세션에서: "PROJECT_NOTES.md 읽고 시작해줘" 라고 말하면 됩니다.

## 무엇을 만드는 중인가

전시장(9:16 세로 TV) 설치용 웹캠 인터랙티브 아트. 관람객이 양손 검지+엄지로 사각
프레임을 만들면, 그 프레임 안의 관람객 실루엣이 브랜드 컬러 파티클로 채워지는 효과.
`index.html` 하나로 완결된 단일 파일 (MediaPipe Hands + SelfieSegmentation 사용).

## 상태 머신 (현재 흐름)

```
IDLE (대기 루프, 손 제스처 Lottie 안내) → ARMING (라이브 뷰 + 리마인더, 0.6초 유지) → ACTIVE (파티클 효과)
```
- `IDLE`: 검은 배경 + 브랜드 12색(전용 `RING_PALETTE`) 도트 링(로고를 감쌈) + Lottie 손 제스처
  애니메이션 안내. 손 제스처 없을 때 항상 이 상태로 복귀
- **`INTRO` 단계는 제거됨** (커밋 `ea7c896`) — 흰 배경 온보딩 화면 없이, 제스처가 인식되면
  바로 라이브 카메라 뷰(`ARMING`)로 진입. IDLE 화면 자체가 사용법 안내 역할을 겸함
- `ARMING`: 라이브 카메라 + 상단 로고 + 하단 작은 리마인더(단순화된 L자 손모양 아이콘,
  `HAND_ICON_SVG`). 손가락 프레임 글래스 패널이 은은하게 숨쉬듯 빛남 (외곽선/스캔라인 없음,
  넓은 radial mask로 배경과 블렌딩 — 커밋 `6c36f4b`)
- `ACTIVE`: 실제 파티클 효과. 로고만 남고 리마인더는 사라짐

## 파티클 시스템 — 핵심 아키텍처

**"고정 앵커" 방식.** 각 파티클(인덱스)은 프로그램 시작 시 `sc`(640×480 마스크 캔버스)
좌표계에 **영구 고정된 자리**를 배정받습니다(`psx`/`psy`). 110ms마다 도는 리빌드는
그 자리를 실루엣 안으로 옮기는 게 아니라, "지금 이 자리가 실루엣 안에 들어와 있는가"만
다시 판정해서 on/off(`pta`)만 바꿉니다. → **앵커 좌표는 절대 이동하지 않고, 켜지고 꺼지기만
합니다.** 이게 지금 구조에서 가장 중요한 불변 원칙입니다.

on/off 판정에 쓰이는 밝기 기반 확률(`pluck`)도 **앵커당 한 번만 고정 roll** — 매 리빌드마다
`Math.random()`을 다시 굴리면 정적인 사람도 앵커의 1/3이 이유 없이 깜빡거립니다 (실제로 이
버그를 겪고 고쳤음, 아래 "겪었던 문제들" 참고).

**EMA 스무딩 + 히스테리시스 (커밋 `b1f7f71`).** 마스크 알파값을 매 리빌드마다 그대로 읽지 않고
`ALPHA_SMOOTH=0.4` 가중치로 지수평활한 뒤, on 기준(`ALPHA_ON=46`)과 off 기준(`ALPHA_OFF=34`)을
**다르게** 적용합니다 — 카메라/세그멘테이션 모델의 프레임 단위 노이즈가 경계선 근처 앵커를
혼자 깜빡이게 만들던 문제를 억제하기 위함. 지속적인 변화만 on↔off 문턱을 넘습니다.

**글라이드 인 (같은 커밋).** 새로 켜지는 앵커는 자기 홈 위치에 바로 팝업하지 않고, 같은
리빌드에서 방금 꺼진 근처 앵커의 마지막 화면 좌표에서 시작해 몇 프레임에 걸쳐 자기 자리로
미끄러지듯 이동합니다 — "하나가 사라지고 다른 게 나타남"이 아니라 "점이 옆으로 슬라이드"로
보이게 하기 위함. **주의: 이건 앵커 좌표(`psx`/`psy`) 자체가 움직이는 게 아니라 화면상
시각적 트윈(`pdx`/`pdy` → 홈 위치로 보간)일 뿐** — "앵커는 이동하지 않는다"는 위 원칙과
모순되지 않음.

## 현재 튜닝 값 (index.html 안에서 검색해서 찾을 것)

| 값 | 현재 설정 | 의미 |
|---|---|---|
| `N_MAX` | 3000 | 전체 파티클 슬롯 수 (640×480 전체 캔버스 기준 그리드) |
| 파티클 반지름 | `(6~25) * scale * (1~2.5배)` | 크기 다양성 (10~30 시도했다가 6~25로 되돌림) |
| `FLOAT_R` | 5px | 각 파티클이 제자리에서 떠다니는 반경 (아주 작게 — 실루엣 깨지지 않게) |
| `PULSE_AMOUNT` | ±18% | 크기 숨쉬기 폭 |
| `DOMAIN_R` | `0.178 * 0.6 * 0.5` | 손가락 프레임 도메인의 모서리 둥글기 |
| `ARM_MS` | 600ms | 제스처 유지해야 ACTIVE로 넘어가는 시간 |
| `REBUILD_MS` | **110ms** (구 200ms) | ACTIVE 상태에서 on/off 재판정 주기 |
| `ALPHA_SMOOTH` | 0.4 | 마스크 알파 EMA 가중치 (낮을수록 더 감쇠/지연) |
| `ALPHA_ON` / `ALPHA_OFF` | 46 / 34 | on 진입 / off 유지 문턱 (히스테리시스) |
| `RING_ROTATE_S` | 25초 | 대기화면 도트 링 한 바퀴 회전 시간 |
| `RING_BREATHE_S` | 7초 | 링 반지름 숨쉬기 주기 |

렌더 루프는 60fps로 상향됨 (커밋 `b1f7f71`).

컬러 팔레트는 두 개로 분리됨 (커밋 `26efd27`):
- `PALETTE` (12색): 파티클 효과 전용, 브랜드 컬러 시트에서 추출, depth 무관 균등 무작위 선택
- `RING_PALETTE`: 대기화면 도트 링 전용, "Extended Palette 2" 브랜드 시트에서 추출 — `PALETTE`와
  독립적으로 바꿔도 파티클 색에 영향 없음

9:16 세로 / 16:9 가로 레이아웃은 로고·태그라인·안내문구·손제스처 폭·링 반지름을 각각
별도 변수 세트로 분리 튜닝 (와이어프레임 기준, 커밋 `de9f68e`/`543db91`).

## 겪었던 문제들 (다시 반복하지 않기 위한 기록)

1. **매 리빌드 랜덤 재배치 → 뭉개짐**: 후보 픽셀을 매번 새로 스캔+shuffle+stride 샘플링하던
   구조에서는, 셔플을 아무리 국소적으로 해도 파티클이 매번 다른 위치로 "점프"해서
   부드러운 모션을 얹으면 실루엣 전체에 흔적이 번지는 문제가 있었음 → **완전히 폐기**하고
   지금의 "고정 앵커" 구조로 교체.
2. **N_MAX 초과 시 다리(하체)가 통째로 사라짐**: 후보가 N_MAX보다 많으면 스캔 순서상 앞부분만
   잘라 쓰던 버그. 지금 구조(고정 앵커)에서는 이 문제 자체가 발생하지 않음.
3. **200ms마다 앵커의 1/3이 이유 없이 깜빡임**: 밝기 기반 포함 확률을 매번 `Math.random()`으로
   다시 굴려서 생긴 문제 → 앵커당 고정 `pluck` 값으로 해결 (위 참고).
4. **도메인 스코프 앵커 (되돌림)**: "도메인 안에 N개가 다 들어가게" 요청으로 손 프레임
   크기에 맞춰 앵커를 그때그때 재배치하는 버전을 만들었다가, 사용자가 롤백 요청 →
   **지금은 전체 캔버스 기준 고정 그리드**로 되돌아간 상태. 즉 작은 손 프레임을 만들면
   그 안에 들어가는 파티클 수는 `N_MAX`보다 적음 (버그 아니라 현재 설계).
5. **오퍼시티 페이드 전부 제거**: 원래 fade-in/out이 있었는데, "나타날 때부터 100%,
   사라질 때도 즉시 0%"로 명시적 요청 → `pa` 배열 자체를 삭제하고 `pta`(0/1) 하나로 단순화.
6. **경계선 근처 앵커의 프레임 단위 노이즈 깜빡임**: 200ms→110ms로 리빌드 주기를 줄이자
   카메라/세그멘테이션 모델의 미세한 프레임 간 노이즈가 실루엣 경계 근처 앵커를 혼자
   깜빡이게 만듦 → EMA 스무딩 + on/off 이중 문턱(히스테리시스)으로 해결 (위 "EMA 스무딩 +
   히스테리시스" 참고). 문제 3번(pluck 재굴림 깜빡임)과는 다른 원인·다른 해결책.
7. **Lottie 손 제스처 애니메이션이 안 보임**: 내보낸 JSON의 정적(`"a":0`) shape-path가
   `"k"`를 배열로 감싼 비정형 포맷이라 lottie-web이 파싱하지 못하고 빈 `<path>`만 렌더링함
   → 로드 직후 재귀적으로 언랩하는 `fixStaticShapePaths()`로 해결 (`index.html` 240행 부근).
   같은 이유로 `autoplay:false` 상태에서는 SVG 렌더러가 프레임 0조차 그리지 않아서
   `goToAndStop(0, true)`로 강제 첫 빌드를 트리거함.

## 알려진 한계 / 미완성 부분

- **ARMING 리마인더 아이콘**: 실제 손 일러스트가 아니라 두 개의 대각선 L자 코너 브래킷으로
  단순화한 `HAND_ICON_SVG`. (IDLE 화면의 손 제스처 안내는 Lottie 애니메이션으로 교체 완료 —
  이 한계는 ARMING 상태의 작은 리마인더 아이콘에만 해당)
- **실제 카메라 미검증 항목**: 이 세션은 샌드박스 환경이라 실제 웹캠으로 최종 확인을
  못 한 부분들이 있음 (특히 9:16 실물 TV에서의 룩앤필, 손 제스처 인식 안정성, EMA/히스테리시스
  튜닝값이 실제 조명·카메라에서도 적절한지).
- **도메인이 움직이면 파티클이 옅어짐**: 위 4번 참고 — 제스처 유지 중 손을 크게 이동하면
  고정 앵커가 새 위치를 벗어나 파티클이 줄어들 수 있음. 손을 뗐다 다시 잡으면 해결됨.

## Git

- Repo: https://github.com/redjun36/Particle-Filter
- 항상 작업 전 `git pull origin main`, 작업 후 `git add -A && git commit -m "..." && git push origin main`
